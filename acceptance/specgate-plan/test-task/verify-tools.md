# 验证手段调用约束（给测试方）

## 1. 视觉 / 交互类手段要先做锚点体检，据实际情况选路线
一次原型验证中的观测：真实项目里显式语义角色标注通常只有个别几个、
测试专用标识常为零、用 div 模拟交互的元素占候选交互元素的 74%~90%。
⇒ 多数项目要走 **unit-visual**（交互单元 + 文本骨架）而不是 **ax**（依赖语义角色）。

## 2. 双跑比对时两侧业务数据必须一致
否则列表类节点的数量差会污染整份报告。

## 3. ⚠️ 必须做故障注入验证
全绿不能证明任何事，可能只是检测器失效。
每次验证都要附一条已知会失败的断言，确认它真的报错。
